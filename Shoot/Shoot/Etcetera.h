enum
{
	ETC_SINE90TXT,
	ETC_DATSTRINGS,
	ETC_JCHAR,

	// app >

	ETC_KOAKUMA_P1,
	ETC_SCRIPT_0001,

	// < app

	ETC_MAX, // num of member
};

oneObjectProto(resCluster<autoList<uchar> *>, GetEtcRes);
